# Copy to .env at repository root before running docker compose.
# Set this to your public app domain (or comma-separated domains).
CLIENT_ORIGIN=https://your-domain.com

# Required in production: use a long random value.
JWT_SECRET=replace_with_a_strong_secret

# Optional redis fan-out for multi-backend deployments.
ENABLE_REDIS=false
